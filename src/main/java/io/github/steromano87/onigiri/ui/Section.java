package io.github.steromano87.onigiri.ui;

/**
 * The main section interface.
 *
 * Sections are defined as logical collections of elements (both simple and composite), so they can be treated
 * as a special case of extended WebElements.
 */
public interface Section extends ExtendedElement, Scrollable {
}
