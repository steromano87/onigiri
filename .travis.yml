language: java

services:
  - docker

# To preserve caches and speed up builds
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

# To load coverage results to CodeClimate
before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build

script:
  - ./gradlew check -Pdockerized

after_script:
  - export JACOCO_SOURCE_PATH=src/main/java
  - export CC_TEST_REPORTER_ID=6251641354bb1574bd4619f8c2ad23ffc5eda9043a52750d7f5f05d0cc3034a0
  - ./cc-test-reporter format-coverage --input-type jacoco build/reports/jacoco/test/jacocoTestReport.xml
  - ./cc-test-reporter upload-coverage
